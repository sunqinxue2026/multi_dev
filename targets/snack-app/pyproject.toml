[project]
name = "snack-app"
version = "0.1.0"
description = "A convenience-first snack shopping app demo"
requires-python = ">=3.10,<3.14"
dependencies = []

[project.scripts]
snack-app = "snack_app.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
